# 本次手动核对记录

核对时间：2026-09-02  
运行环境：Spring Boot 2.7、MySQL 8.0、`http://localhost:8660`

本文件只记录本次实际完成的手动核对，不写没有执行过的结果。

## 1. 启动情况

- Maven 编译成功。
- 服务可以连接本地 MySQL 并在 8660 端口启动。
- 登录页和主页都能正常打开。
- 前端脚本没有语法错误，浏览器控制台没有报错。

## 2. 登录与密码

- `admin / 123456` 登录成功，角色为管理员。
- `manager / 123456` 登录成功，角色为宿舍管理员。
- `student / 123456` 登录成功，角色为学生。
- 数据库中没有明文密码记录。

## 3. 权限与数据范围

- 未登录访问床位接口返回 401。
- 学生访问用户列表返回 403。
- 学生即使在地址中传入其他学生 ID，账单和入住申请仍只返回本人数据。
- 学生申请退掉别人的床位返回 403，没有修改数据。
- 宿管新增楼栋返回 403；楼栋、楼层、宿舍和用户维护仍由管理员负责。
- 宿管查询用户时只返回学生，用于床位分配和账单录入。
- 宿管不能跨楼栋发布指定楼栋公告。

## 4. 页面显示

- 学生看不到“系统数据”页签，也看不到床位分配、腾空、编辑和删除按钮。
- 学生退宿位置由下拉框显示自己的当前床位，不再手填床位 ID。
- 宿管能看到床位和账单操作，但看不到楼栋、楼层、宿舍及用户新增表单。
- 管理员能看到全部管理表单。
- 三类角色名称和各业务状态均显示为中文。

## 5. 本次没有做的内容

- 没有加入课程演示不需要的额外组件。
- 项目按本地单实例运行进行说明。
- 没有编写任何测试代码或加入测试依赖。
- 为避免污染演示数据，本次只对会被拒绝的异常请求做了手动核对，没有批量新增或删除业务数据。

后续答辩演示可直接按 `页面端点击顺序清单_验收模板.md` 的顺序进行。

## 6. 文件与依赖清理

- 项目数据库只保留 MySQL 8.0 的驱动和连接配置。
- 已清理控制器读取副本、Maven 生成目录、日志和其他临时文件。
- 正式控制器只保留 `src/main/java/com/dorm/demo/web/DemoController.java`。

## 7. 宿舍偏好推荐补充（2026-09-14）

- 学生端已增加氛围、作息、卫生和兴趣四项偏好。
- 后端按固定分数推荐最多 3 个有空床的宿舍，并返回分数和推荐原因。
- 学生可采用推荐，也可以改选其他意向宿舍；推荐不会自动审批或直接分配床位。
- 管理员可维护宿舍特点，管理员和宿管可在入住申请中查看学生偏好及意向宿舍。
- 本次完成了 Maven 编译和前端 JavaScript 语法检查，没有在本报告中补写未实际执行的页面点击结果。

## 8. 注册与页面排版补充（2026-09-14）

- 登录页已增加基础的新生注册入口，注册时只填写用户名、姓名和密码，账号身份固定为学生。
- 注册成功后回到原有登录流程，没有增加验证码、短信、邮箱或新的角色体系。
- 管理表单、入住退宿表单、看板卡片和推荐卡片统一了宽度、间距及控件排列，原有页面结构和技术栈没有改变。
- 本节只记录已完成的代码修改；页面点击结果需按手动验收清单实际操作后再填写。
